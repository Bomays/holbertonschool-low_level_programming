#include "main.h"
/**
 * add -  function that adds two integers and returns the result.
 *
 * @a : first integer a
 * @b : second integer b
 *
 * Return: return the result of a + b
 */
int add(int a, int b)
{
	return (a + b);
}
